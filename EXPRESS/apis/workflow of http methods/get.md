What happens internally

Client sends request

Server receives GET request

Express matches route /books

Server returns data

Flow:

Client
  ↓
GET /books
  ↓
Server finds route
  ↓
Server returns books